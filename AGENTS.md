# BHHS Demo 工作约定

## 已确定的目标与范围

- 本项目是 BHHS Gulf Properties 房产销售辅助 Demo；EnerMech 仅作为代码与交互参考。
- 既定主链路：销售整理客户需求 → 房源库筛选或自然语言助手匹配 → 房源详情 → 当前价格、历史成交与周边可比价格 → 潜在客户比较 → 辅助销售沟通。
- 控制塔任务负责范围、依赖和验收；开发任务负责实现。产品 A 负责房源、成交与关联证据；产品 B 负责客户样本、判断依据和业务验收。
- 不重新讨论已确定主链路。低风险实现选择自行推进；关键范围矛盾记录并报控制塔。
- 不继承 EnerMech 的油服字段、Project Gate、固定 26 字段导出和页面数量限制。
- 物业管理、完整 DataOS/本体平台、CRM 写回、自动外发消息、生产级持续采集及公网发布不在当前授权范围。

## 证据与数据

- 明确区分事实、会议或用户陈述、推断、建议和 Unknown；不能把公开可见等同于可任意复用。
- 挂牌价、实际成交价、同一套房的历史成交、相似房源的可比成交分别处理。
- 房源下架不等于已成交；交易 ID 不等于房屋 ID；同楼同面积不构成同一套房的充分证据。
- 无依据的字段留空。没有经验证的样本和判断规则时，不生成精确成交概率或推断客户资产。
- 产品填写模板见 `docs/data-contract.md`、`data/templates/` 和 `outputs/01a06522-fb17-7d92-9e65-14eaf803b75e/`。
- 用户或产品交付的真实数据先存 `data/incoming/` 或 `data/private/`；这两处不进入 Git。只提交空模板、明确标注的演示数据和经审查可版本管理的资料。
- 模板中的演示说明不是 BHHS 客户事实，也不能作为产品已确认规则。

## Git 留痕：用户明确要求小步提交

- 开工先检查 `git status --short`、当前分支、最近提交。若有他人修改，保留并明确责任边界。
- 每完成一个可独立检查的改动即提交，不积累到整个 Demo 完成才一次提交。建议按脚手架、数据契约、房源库、助手、详情与价格、客户比较、修复与验收分段。
- 提交前检查改动范围和 `git diff --check`，运行与本次改动风险相匹配的验证。小文案/模板变更无需跑全套应用测试；涉及交互时验证实际操作。
- 使用明确路径 `git add <本次文件>`；检查 `git diff --cached --name-only` 和暂存差异，不直接全量暂存陌生改动。
- 提交标题格式：`feat|fix|docs|test|chore(scope): 具体变化`。正文记录 `Changes`、`Validation`，有未完成项时记录 `Remaining`。使用文件传递多行提交消息。
- 不把未验证改动说成通过。必要的阶段存档可明确使用 `chore(wip)`，写清未通过检查和剩余工作，后续用新提交修复。
- 交接、长暂停及阶段完成前检查未提交内容；完成的本次改动应已提交。不能为了追求 clean 擅自暂存别人的文件。
- 提交后核对短 SHA、提交文件清单和 `git status --short`，在进度/交接中报告 SHA、变化和验证结果。
- 保留历史；没有明确授权不 amend、squash、reset --hard、force push 或重写历史。回退已提交内容优先使用新的 revert 提交。
- 用户当前授权本地 commit；没有明确要求不自动 push，不自行创建远端或公网部署。
- 控制塔和开发若共享目录，提交必须串行：不得在另一方暂存或提交过程中插入自己的 Git 操作。跨分支/多写者改动先明确文件归属。

## 秘密与产物

- 密钥、令牌、密码、未脱敏客户数据、依赖目录、构建缓存和临时 QA 产物不得进入 Git。
- 禁止打印密钥；`.env.example` 仅含变量名和空占位。
- 记录可复现的启动方式、实际验证和已知限制，不以截图或构建通过代替业务链路验收。
