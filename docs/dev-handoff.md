# BHHS Demo 开发交接

## 当前状态

最新本地改版增加 Home 规则助手、演示销售身份、公私客户目录、房源面积区间和列头排序、Reports 房屋证据与本机看房记录。新增和审核后的需求继续独立保存，并按 Sales ID、来源及五表内容版本隔离。页面刷新恢复有效需求/详情链接，支持删除与恢复原件；原文冲突、缺口径与待补信息保留。

当前分支 `main`。本轮依据附件改版清单及用户“启动”授权，从干净的 `5f90622` 开始，完整保留此前面积审核与本机保存提交。本轮小步 SHA、实际操作和边界见文末“Home、销售身份与 Reports 改版”。交接文档最后独立提交，最终 SHA 以 `git log -1 --oneline` 和窗口交接为准。所有 Git 操作由主代理串行完成，未暂存未知修改。

本地独立批次地址 **[http://127.0.0.1:5174](http://127.0.0.1:5174)**，默认主演示为 **[http://127.0.0.1:5173](http://127.0.0.1:5173)**。49 条 demo 接收记录保持原样，28 条公开资料仍隔离；15 条人工参考均 draft，6 条推荐/备选仍待客户面积口径确认。**技术交互通过不等于这 15 条业务验收通过。** 使用方式见 [本机保存](local-requirements.md)、[报告记录](report-records.md)，新版输入准备见 [案例复验](case-revalidation.md)。

**本轮只在本地完成，未 push、未部署。** 此前按用户追加授权发布的 [在线 Demo](https://bhhs-gulf-properties-demo.vercel.app) 及当时的匿名验证记录保留在历史章节，不含本机保存与本轮改版增量。当前所有业务输入、演示设定与确认统一收口给用户；控制塔整理资料与验收准备，原产品 A/B 表格保留为历史分工。

## 启动与地址

工作目录 `E:\bhhs`，在 PowerShell 执行：

```powershell
npm install --cache .work/npm-cache
npm run dev
```

按锁文件重装可用 `npm ci --cache .work/npm-cache`。本机默认 npm cache 的 junction 曾失效，安装采用 `.work/npm-cache` 避免该环境问题；没有要求变更全局 npm 配置。

- 网页：`http://127.0.0.1:5173`
- API：`http://127.0.0.1:8001`
- 检查：`GET /api/health`；数据：`GET /api/dataset`
- 独立启动：`npm run dev:web`、`npm run dev:api`；组合启动用 `npm run dev`，`Ctrl+C` 停止。
- 两个本地服务只绑定 `127.0.0.1`；在线版本通过独立 Vercel 项目提供虚构演示快照。

## 代码仓库

用户此前于 2026-09-03 UTC 授权新建公开 Git 仓库并同步当时提交。仓库为 [Ksaveworld/bhhs-gulf-properties-demo](https://github.com/Ksaveworld/bhhs-gulf-properties-demo)，`origin` 使用 HTTPS，主分支 `main`。面积口径轮按追加上线授权推送；后续本机保存与改版轮只做本地提交，历史保留，未强推或改写提交。

首次推送前检查了 16 次提交、76 个唯一文件 blob 及 ZIP/XLSX 模板：凭证模式扫描无命中，历史未跟踪 `data/incoming/`、`data/private/`、`.env` 或临时 QA 目录，业务 Excel 数据区为空。此检查不是对未来提交的持续保证，真实资料仍按私有目录约定处理。

## 已实现内容

| 主链路环节 | 当前实现 | 边界 |
|---|---|---|
| 统一房源库 | 地区、AED 预算、卧室/类型、sq ft 面积上下限等即时过滤；价格/更新时间列头双向排序 | AED 搜索视图；原其他币种在来源/报告中保留；缺失字段不确认满足硬条件 |
| 销售需求与助手 | Home 直接填写或粘贴内容，规则提取后审核/编辑再保存并应用 | 无模型调用；保留原文、缺口与未支持条件 |
| 同源候选 | 手动筛选、需求应用、客户候选均调用共享确定性过滤 | 解释层展示匹配、冲突和 Unknown，不把偏好或缺失条件当作事实 |
| 房源详情 | 正确挂牌入口、原币种价格、面积口径、来源、时间及挂牌快照 | 旧挂牌快照不进入成交时间轴 |
| 价格证据 | 同屋图/时间线、日期筛选、来源回跳；可比成交独立；抵押、部分份额等排除 | 按唯一交易计数，币种/日期口径分系列；不做平滑、估值或成交预测 |
| 双向匹配 | 客户按人去重，每份需求独立；预算/时间透明排序、理由和待补信息 | 不合并需求，不生成成交百分比，不推断资产或支付能力 |
| 客户目录与身份 | Name / Budget Range / Preferred Location 明显可见；Company / Private 标签与筛选，私有副本属于当前 Sales ID | 本机演示身份，不是生产认证；旧无身份副本保持旧分区 |
| Reports | 两个 Tab；房屋摘要及成交图，客户看房时间线、次数、主动偏好标签和同预算观察 | 本机销售记录与虚构示例标记来源；不训练模型、不把到访数当作成交意愿 |
| 数据状态 | 加载、空结果、缺字段、错误、数据重试及隔离说明 | 已实际操作验证，详见下面验收记录 |

点击应用且确认保存后，需求独立副本保存在当前浏览器。刷新或 Refresh data 重新读取 API，恢复地址中仍有效的需求和详情；临时手动筛选重置，同一来源/版本/身份的副本仍可选用。没有服务器备份、CRM 写回或自动发送消息。

## 文件职责和字段关系

- `apps/web/src/`：英文 React / Ant Design 界面，房源主入口与需求、详情、客户比较交互。
- `shared/types.ts`：保持模板 snake_case 字段；从显式 ID 派生客户和房屋，不用挂牌 ID 猜房屋身份。
- `shared/matching.ts`：确定性硬条件过滤、面积单位换算、排序与条件解释，供所有候选入口复用。
- `shared/assistant.ts`：`AssistantAdapter` 接口；当前规则实现用于结构化草稿，没有声称已接入大模型。
- `shared/pricing.ts`：只把合格记录分类到同屋历史或可比成交；不做精确估值。
- `shared/client-priorities.ts`：按 `client_id` 分组，每份 `requirement_id` 独立调用原 `evaluateMatch`，显示预算差额、入住时间相容性并透明排序。
- `shared/transaction-history.ts` 与 `TransactionHistory.tsx`：消费 `getPriceEvidence().history`，按交易去重、币种/日期口径分系列，提供图点、日期筛选和原始来源定位。
- `apps/api/ingest.mjs`：按当前 v1.1.0 schema 读取/校验 CSV 或 JSON，兼容缺少客户面积口径列的 v1.0.0 输入，隔离不合格记录及其依赖。
- `apps/api/server.mjs`：Node 原生 HTTP、只读 GET、本机绑定、每次请求重新读取。
- `data/demo/`：可替换的完全虚构样例及生成脚本；`data/incoming/`、`data/private/` 为不进 Git 的真实输入位置。

数据契约使用五张表，区分以下实体与关系：

| ID / 表 | 含义与关联 |
|---|---|
| `client_id` / `requirement_id` | 脱敏客户可以有多份需求；不能以电话或邮箱作为客户 ID |
| `property_id` / `listing_id` | 房屋身份与挂牌身份分开；房屋 ID 仅在证据可靠时填写，允许一房多挂牌 |
| `snapshot_id` / `listing_snapshots` | 一次挂牌观察一个稳定快照 ID；同挂牌更新增加快照 |
| `transaction_id` / `transactions` | 来源中的交易记录，不等于房屋身份 |
| `link_id` / `listing_transaction_links` | 显式关联挂牌与交易；标明 exact_property、comparable 或 unresolved |
| `case_id` / `match_reference` | 产品人工期望结果，引用需求、挂牌及合格价格关联；不作为助手输出 |

金额、币种、面积值/单位/口径、来源、采集时间、核验和许可分别保留。多值 CSV 字段用 `|`，应用内为数组；空值为 null，不用 0 代替 Unknown。

## 样例性质与覆盖

默认 `data/demo/dataset.json` 为 **10 个挂牌 / 11 个快照、8 个交易、9 个关联、6 位客户的 8 份独立需求、8 条人工参考**；全部 `data_kind=demo`，来源 `Synthetic demo fixture` / `DEMO-*`。其中的 verified、approved、confirmed 仅表示演示样例的技术状态，不代表产品或真实业务核验。

- `DEMO-L-001`：虚构 Marina ready 两居，最新 AED 2,450,000、1,280 sqft built_up；旧挂牌快照 AED 2,550,000。
- `DEMO-R-001`：虚构 Marina apartment / ready / 至少两居、AED 2.2m–2.8m、至少 1,100 sqft built_up，候选 `DEMO-L-001/002`。
- `DEMO-LINK-001`：虚构同屋成交；`DEMO-LINK-002`：不同房屋可比；`DEMO-LINK-003/005`：抵押/部分份额排除案例。
- `DEMO-L-007` 无价格，`DEMO-L-008` 无面积，`DEMO-L-009` 下架但未断言成交，`DEMO-L-010` 使用 USD。
- `DEMO-R-004` 无匹配，`DEMO-R-006` 信息不完整；`DEMO-R-001/002` 可对应同一 `DEMO-L-001`；`DEMO-L-005/006` 无成交历史。
- 第二轮新增 `DEMO-R-007/008` 为 A/B 原客户的另一份独立需求，原需求保留。L001 为 2 位条件满足、2 位待补、2 位硬冲突；L002 中 B 的 R002 超预算但 R008 满足，验证不能合并需求或重复计人。
- L001 的虚构同屋成交为 T007（2020-03-12，AED 1,650,000）、T008（2022-08-06，AED 1,920,000）、T001（2024-11-20，AED 2,100,000）。L004 只有 T006 一笔，L005/006 无历史。新增两笔与关联只用于覆盖多点图，不是 BHHS 真实成交。

样例是覆盖交互的测试材料，不是 BHHS 客户事实、真实估价或已确认产品规则。EnerMech 仅作为工程和交互参考，没有继承油服字段、Project Gate、固定导出字段数或页面数量限制。

## 产品接入与数据安全边界

填写格式沿用 [数据契约](data-contract.md)、[模板说明](../data/templates/README.md)、[schema](../data/templates/schema.json) 和 [字段字典](../data/templates/field_dictionary.csv)。详细步骤见 [导入指引](import-guide.md)。接入 key 使用 CSV 英文表头，新增或修改共享字段先向控制塔说明；不让产品覆盖演示目录。v1 契约不代表数据库架构或业务规则已冻结。

```powershell
$env:BHHS_DATA_DIR = 'data/incoming/批次名称'
npm run dev
```

目录内放五份契约 CSV，或直接指定五表 `dataset.json`；首版不直接导入 XLSX。缺少某张表时该表为空并提示，其他合格表继续加载。环境变量变化需重启；替换文件后刷新数据。解析错误会返回安全错误，不沿用旧缓存，不泄露原始隔离字段值。

校验包括：必填、类型、枚举、稳定 ID 唯一、完整日期和时区、有限非负数字、预算范围、金额币种和面积单位/口径。真实数据必须 verified / approved，人工验收参考必须 confirmed；不合格行及跨表依赖级联隔离。

**最新快照规则：**先看全部输入的 `captured_at`，再判断最新记录能否接受。最新被隔离或时间顺序无法确认时，同一挂牌全部暂扣，旧 active 不会回退成当前库存。旧快照的一般字段错误不阻止合格最新快照。

**价格证据规则：**同屋历史要求匹配的稳定 `property_id` 和两侧来源引用；同楼或同面积不足以建立身份。两侧已知同一 `property_id` 却标 comparable 时隔离，不自动改分类。价格参考要求 sale / whole_unit、金额日期明确、来源和关联已核验、使用允许、币种及面积口径可比；抵押、租赁、赠与、打包、部分份额或 unresolved 不用于住宅出售价格对比。

## 待补输入与决策负责人

| 输入或事项 | 负责人 | 后续处理 |
|---|---|---|
| 真实可使用房源、挂牌快照、来源与许可 | 产品 A | 交付完整 CSV；不能把公开可见等同于可复用 |
| 成交样本、稳定房屋身份和关联依据 | 产品 A | 提供同屋/可比/排除案例及可追溯证据 |
| 可比距离、时间、面积偏差、显著差异等阈值 | 产品 A，控制塔协调 | 确认后再决定计算与提示；目前不宣称已校准估值 |
| 授权脱敏客户需求、人工期望结果、业务案例 | 产品 B | 验证一客多房、一房多客、预算冲突、无匹配、无历史、信息缺失 |
| 硬条件与偏好的解释、面积口径、意愿证据 | 产品 B | 销售审核；不把规则样例当作业务已确认 |
| 意愿等级、综合优先级或规则分的定义和正反例 | 产品 B，控制塔确认 | 当前只展示意愿原文与透明排序；没有权重、规则分或概率模型 |
| 同客多需求与多客先后顺序的人工验收基准 | 产品 B | 使用现有 client_requirements / match_reference，注明各判断对应的需求和理由 |
| 多笔、单笔、无历史的真实同屋成交证据 | 产品 A | 现有 transactions / listing_transaction_links 足够，无需为曲线扩展契约 |
| 是否需要房源负责销售及挂牌变化图 | 控制塔，产品 A | 未实现；客户需求 sales_owner 不能当作房源销售归属。确需新增字段先确认契约增量 |
| v1 未结构化的卧室/面积上限和否定条件 | 销售确认，产品 B/控制塔处理后续规则 | 保留原文和 Unknown，不反向当作下限，不由开发静默扩展契约 |
| 最终业务验收 | 控制塔结合产品 A/B | 开发提供实现及技术证据，不替代业务确认 |

v1.1.0 需求已有选填 `area_basis`。旧 v1 输入仍兼容 `hard_constraints` 的 `area basis: built_up`（或 internal/gross/land），字段明确时优先，冲突时保留原值并提示确认。单位、口径未知时不能确认面积满足。不接入汇率前仅按原币种保留和比较。没有模型配置时继续规则模式，后续授权模型可替换 adapter；不改变确定性过滤、计算和排序职责。

## 第二轮实现口径

- 一个 `client_id` 只计一位客户；每份需求独立计算原有匹配结果。客户归入其最佳单份需求的条件状态，摘要注明相应 `requirement_id`；展开后仍能查看其他需求及其预算、冲突和来源，分别返回候选房源。
- 三种排序都先按满足条件、待补信息、硬冲突分组。默认同组按客户 ID 稳定排序；预算排序先覆盖报价，再按到预算区间的绝对差额升序，未知最后；购买日期排序为最早日期优先，未知最后。同组并列按客户 ID。同一客户的摘要也按当前排序选取最佳状态中的一份需求，字段不混合。
- 预算只使用客户明示范围，显示原币种差额，不做资产推断或汇率折算。购房日期只作排序；房源可用时间仅与明确的 `move_in_by` 比较，不能把购买截止日期当作入住截止日期。意愿展示具体沟通证据，不映射高低、规则分或概率；`match_reference` 未参与计算。
- 成交图、时间线与原始明细共享去重后的合格同屋记录；笔数按唯一 `transaction_id`。重复关联不增加笔数；同日同价的不同交易保留独立记录，共用带笔数的图点并可逐笔访问。
- 图的 X 轴为真实日期间距，Y 轴为原币种总价、零基线；币种与合同/登记日期口径分系列，切换系列清空日期范围。默认为空的日期边界代表当前系列全部收录历史，页面显示实际可用日期；错误或无记录范围可重置恢复。多点连线仅用于阅读，没有平滑拟合、估值或日期补齐。
- 点击、Enter 或 Space 可打开原成交记录并展开来源；聚合点激活后焦点进入对应交易选择按钮，Tab 可访问同组下一条。原始明细倒序、图与时间线正序，均对应同一组交易 ID。
- v1 契约、`getPriceEvidence` 和导入资格门槛未改。跨币种图系列通过前端合成响应注入验证，**不代表当前 CSV 导入已允许跨币种价格参考**；导入仍保持原来的币种/面积口径要求，后续放宽需单独确认。

## 首轮历史验证记录

以下是首轮 `76b91a4` / `9c0206f` 时的记录，保留原始样本数量与结果；第二轮当前结果见后续章节。

验证日期：2026-09-03 UTC（本机 2026-09-02）。主代理已在当前工作区重跑，并重启本地服务以加载最终代码。

| 验证项 | 结果 | 证据边界 |
|---|---|---|
| `npm test` | **33/33 通过，退出 0** | 导入/API 13 项，匹配/规则 20 项；包括真实 HTTP 文件替换、来源许可、最新快照隔离和价格证据门槛 |
| `npm run build` | **通过，退出 0** | 先通过 TypeScript 检查，再构建 3006 个模块；主 JS 1,010.18 kB、gzip 318.06 kB，有大于 500 kB 提示，未做代码拆分 |
| `npm run test:browser` | **9/9 通过，45.1 秒，退出 0** | 本机 Chrome；主视口 1440×1000，另验 1366×768；测试脚本在 `tests/browser/sales-flow.spec.ts` |
| 实际服务复核 | **healthy / demo / rules，隔离 0** | `http://127.0.0.1:5173/api/health`；明确没有模型调用；代码更新后已重启服务 |
| 视觉及控制台复核 | **无页面横向溢出，详情操作 pageErrors=[]** | 实际 1440×1000 房源库；详情抽屉 900×1000，可滚动查看证据，截图只作辅助 |
| 控制塔与产品业务验收 | **待输入与确认** | 不由构建、截图或开发单测替代 |

九项浏览器用例的实际行为：

1. 默认 9 个 active 房源；选 Dubai Marina 变 3 个，预算上限 2.8m 变 2 个、2.5m 变 1 个，100 变无匹配；期房筛选返回 L006。
2. 手工筛选与审核后的规则助手均返回 L001/L002；审核和应用后的预算可修改，结果实时变化。
3. L001 详情为 AED 2,450,000、1,280 sq ft、P001；T001 只在同屋区，T002 只在可比区；6 份客户需求有理由，点击客户 A 返回 L001/L002。
4. L008 缺面积且无成交历史；L009 下架显示 retained/recorded asking price，不显示为已成交。
5. 1,300 sqft 与 120.774 sqm 在相同 built_up 口径下返回 L002；未知面积口径不匹配，倒置预算有错误和重置入口。
6. 拦截真实数据请求验证加载；503 刷新错误移除旧结果，解除故障后 Retry loading 恢复。
7. 1366×768 下完成客户导航、需求提取、预算修改、应用及详情价格页；页面无横向溢出。
8. 升序按原币种分组；USD 房源详情仍为 USD 1,250,000；无匹配客户 D 显示 0 个候选。
9. 接口空数据响应显示房源和客户空状态，恢复后 Refresh data 重新显示 6 份需求。

复现命令见 README。临时 JSON 报告、失败诊断和辅助截图保存在忽略目录 `.work/`，不进入 Git。未执行范围：真实产品数据业务验收、真实模型调用、公网性能、多浏览器及移动端完整链路。当前仅做本地开发技术验收。

## 第二轮实际验证记录

验证日期：2026-09-03 UTC（本机 2026-09-02）。应用内容对应 `3e364e0`，新增浏览器测试留痕于 `47e1d6f`。浏览器用例由验收子代理执行新增部分，主代理执行原有回归和补充桌面复核；提交始终由主代理串行完成。

| 实际执行 | 结果 | 范围与限制 |
|---|---|---|
| `npm test` | **53/53 通过，退出 0** | 原 33 项 + 客户分组/排序 11 项 + 成交处理 9 项；原导入与价格资格校验保持通过 |
| `npm run build` | **通过，退出 0** | TypeScript 检查通过；3010 模块；主 JS 1,028.74 kB、gzip 323.46 kB；保留大包提示，无新增图表依赖 |
| `npx.cmd playwright test tests/browser/iteration-02.spec.ts --reporter=list` | **6/6 通过，18.7 秒** | 新增交互全套，含 1366×768 的成交图日期筛选、来源访问和空范围恢复 |
| `npx.cmd playwright test tests/browser/sales-flow.spec.ts --reporter=list` | **9/9 通过，27.9 秒** | 原有普通筛选/助手一致性、价格单位、缺字段、无历史、错误重试、双向回跳及桌面操作 |
| 1440×1000 补充桌面复核 | **通过** | 实际打开客户页和成交图；键盘 Enter 打开 T008 原始来源；页面宽度 1440，无横向溢出；`pageErrors=[]` |
| `GET /api/health` | **ok / demo / rules，隔离 0** | 当前本地服务可访问，仍为虚构数据和规则助手；API 代码本轮未修改，不需要因 UI 变更重启 |

新增六项浏览器用例分别覆盖：

1. 同客多需求只计一人、三组人数、冲突默认折叠；L002 中 B 的 R002 超预算而 R008 满足，按原 R002 返回 L001。
2. 条件/预算/日期排序真实改变顺序；未知靠后且不跨越条件组；意愿缺失有提示，没有百分比。
3. 三笔历史的日期、原币种金额、图点与时间线一致；鼠标与键盘打开原始来源；空范围、逆序范围和重置可恢复。
4. L004 一笔只显示单点；L005 零笔显示空状态，无曲线。
5. 前端注入不同币种、合同日、重复关联及不合格记录，验证分系列、不重复计数，以及挂牌和可比价格不入同屋曲线；未放宽 CSV 导入门槛。
6. 中间的同日同价聚合点保留两笔交易；Enter 移焦首个交易按钮，Tab 到第二个，Enter 打开对应来源。

过程中的初次失败已定位和修复：排序测试多余 Escape 误关抽屉、原生 select 的测试标签定位不准；之后完整新增六项重跑通过。代码审查发现聚合图点的键盘焦点可能被后续图点覆盖，已修复并由第 6 项实操验证。上述失败没有被计为通过。

临时诊断、截图和补充验证脚本保存在忽略目录 `.work/`。没有执行真实客户/成交数据业务验收、真实模型调用、移动端完整链路、多浏览器或公网性能测试。既定主链路及真实性边界未变，后续数据和评分规则缺口见负责人清单。

## 提交与工作区交接

| 已记录提交 | 内容 |
|---|---|
| `6e76328` | 本地 React / API 开发脚手架 |
| `6f4f75a` | 数据接入、虚构样例及安全隔离 |
| `2b7652d` | 共享匹配、规则助手及价格证据门槛 |
| `d17013d` | 销售需求审核与共用筛选表单 |
| `330f03a` | 详情、价格证据与潜在客户比较 |
| `23c1808` | 英文房源主工作区与双向候选联动 |
| `76b91a4` | 九项浏览器实操验收及最终技术验证 |
| `6ba112e` | 第二轮客户按人去重、独立需求及冲突分组，新增虚构多需求案例 |
| `6f6fe9b` | 条件、预算覆盖与购买日期的透明排序 |
| `3e364e0` | 合格同屋成交图、时间线、日期筛选、唯一笔数和来源/键盘操作 |
| `47e1d6f` | 六项新增 Chrome 实操回归；原有九项仍通过 |

开发过程沿用本地小步提交；用户随后明确授权创建上述公开仓库并推送现有历史。提交和推送由主代理串行完成，不重写历史；子任务不执行暂存或提交。

第二轮开工为 `main@ba64798`，工作区干净。当前改动归属开发及其子代理，已按上述提交留痕；没有修改 `.gitignore`、模板、控制塔数据契约或迭代 brief。最终 README/交接文档单独提交，最新 SHA 与提交后工作区状态以开发窗口最终实查交接为准。没有暂存未知修改。

前两轮开发未包含物业管理、完整 DataOS/本体平台、CRM 写回、自动外发、生产级持续采集、销售简报独立导出页和公网发布。用户随后追加了公开部署授权，实施记录如下；其余范围未变。

## 公开部署增量交接（2026-09-03 UTC）

用户明确要求“vercel 新链接，要免登录”。因此新建独立项目并公开发布演示样例；没有修改其他 Vercel 项目、共享数据契约或产品规则。

- 稳定访问地址：[bhhs-gulf-properties-demo.vercel.app](https://bhhs-gulf-properties-demo.vercel.app)。
- Vercel 项目：`kwillsaveworld/bhhs-gulf-properties-demo`；项目 ID `prj_NV8Y2HxWVz6yoLYERbe6uQY0XeuA`。
- 部署实现：`9f6eded`，`feat(deploy): 配置 Vercel 公开演示构建与只读数据快照`。代码和验证变更均已推送 `origin/main`。
- 首次验证部署：`dpl_9jxFVdSRcSXBiLHhiSB7bnXecQkn`，对应 Git SHA `9f6eded1be249f9e0f039a263ae3d1272de6e061`，target `production`、状态 `Ready`。
- GitHub 集成已连接；后续 `git push origin main` 会触发该独立项目构建。部署记录文档单独提交，其最终 SHA 以 `git log -1 --oneline` 为准，避免改写已验证实现提交。
- 实查保护配置为 `ssoProtection=null`、`passwordProtection=null`、`trustedIps=null`，匿名浏览器没有登录、预置登录态或绕过参数。

### 构建与数据边界

`npm run build:public` 执行网页构建后，调用 `tools/export-public-demo.mjs` 复用现有 `loadDataset` 校验，强制只读取 `data/demo/dataset.json`。要求 `meta.mode=demo`、所有表全部 `data_kind=demo`、隔离数为 0，否则导出失败。测试覆盖本机 `BHHS_DATA_DIR` 指向私有位置时仍不读取该位置。没有改动导入门槛、同屋证据资格或共享匹配逻辑。

输出目录为 `apps/web/dist`；Vercel 将原 `/api/dataset` 和 `/api/health` 路径映射到构建产出的 JSON。公开版本没有运行本地 Node API，不支持在线上传或持久化客户资料。页面刷新读取已发布快照，不读取本地产品文件；`Dataset prepared` / `loaded_at` 为快照生成时间，`delivery=static_demo_snapshot` 明确健康路径也是静态发布信息。

使用 `.vercelignore` 白名单仅上传必要源码、schema 和演示数据。部署前实际执行 `vercel deploy --dry --json`，清单为 28 个文件、408,444 bytes；不包含环境文件、真实输入、内部文档、outputs 或 QA 产物。Git 忽略 `.vercel/` 与 `.env.local`；CLI 创建的本机绑定和环境文件不提交、不输出内容。

### 实际验证

| 实际执行 | 结果 | 证据边界 |
|---|---|---|
| `npm test` | **57/57 通过，退出 0** | 原 53 项及公开数据导出的 4 项检查 |
| `npm run build:public` | **通过，退出 0** | TypeScript、Vite 和固定演示快照导出通过；主 JS 1,028.89 kB，gzip 323.49 kB，保留大包提示 |
| Vercel inspect + GitHub deployment API | **Production / Ready** | 首次部署 source SHA 与 `9f6eded` 一致；不是仅依据本地构建成功 |
| 全新匿名 Chrome 线上实操 | **5/5 通过，25.6 秒** | 普通筛选/助手一致、客户去重和独立需求回跳、多笔历史/日期范围/来源、0/1 笔历史、模拟 503 后恢复真实线上数据 |
| 独立匿名访问复核，1366×768 | **通过，退出 0** | 初始 cookies 为 0；首页 HTTP 200、两个 API JSON 均 HTTP 200，未重定向；记录全部为 demo；刷新后 9 个候选、`pageErrors=[]` |
| 静态资源与页面刷新 | **通过** | JS/CSS 首次均 200，刷新后 304 缓存；刷新后界面仍可用。截图仅作为上述操作的补充 |

线上实操复现命令：

```powershell
$env:BHHS_E2E_BASE_URL = 'https://bhhs-gulf-properties-demo.vercel.app'
npx.cmd playwright test --grep 'ordinary filters and reviewed assistant|a property counts|multi-sale history|a single recorded sale|loading and failed refresh' --reporter=list
Remove-Item Env:BHHS_E2E_BASE_URL
```

补充诊断如实保留：首次 Node `APIRequestContext` 直连接口超时，而匿名 Chrome 页面及页面内无凭证 fetch 正常；后续接口验证走同一浏览器网络路径。补充脚本曾错误要求刷新静态资源也为 200，诊断实际为正常的 304 缓存响应，修正断言后全程通过。临时脚本、JSON 结果和截图均在忽略目录 `.work/`，不计入应用改动或正式发布文件。

当前仍为虚构样例与规则助手，产品 A/B 数据、人工基准及业务规则缺口见上方负责人清单。本次没有执行真实数据业务验收、真实模型调用、公网性能/地区可达性、多浏览器或移动端完整测试，不把上线等同于这些事项通过。

### Git 和剩余事项

本次实现修改归属开发主代理及其受控子任务；所有暂存、提交与推送串行完成。`9f6eded` 包含部署配置、导出守卫及其测试、通用界面文案和线上测试地址参数；本节及 README 为随后独立文档提交。没有暂存他人或未知修改；交接时再核对工作区与远端 SHA。

下一步由产品 A/B 交付真实资料和人工匹配参考，控制塔负责业务验收。公开数据源变更需先审查可公开内容，不会因本机替换了输入文件自动发布真实资料。无需为本轮演示新增数据库、模型服务或持续采集。

## 本轮客户口径与独立批次交接（2026-09-03 UTC，本地开发阶段）

本轮读取 `AGENTS.md`、`docs/data-intake-handoff.md` 及批次接入检查报告后推进，保留原件、主演示、原模板及既有历史。以下记录保留当时本地开发阶段的验证结果；其后的追加上线授权与执行记录见文末。

| 提交 | 本轮内容 | 当步实际验证 |
|---|---|---|
| `45f4cb7` | 客户选填 area_basis、解析接口、v1.0.0/v1.1.0 版本化 CSV/schema/字典及新版 Excel | 22 项字段、导入及原有校验通过；TypeScript 通过；新版 Excel 七表和空数据区、121 字段映射及预览检查，旧 Excel 哈希不变 |
| `eefe61b` | 审核已有需求、新建同客会话需求、保留原文/硬条件、共享面积过滤和待确认说明 | 35 项相关测试通过；新增 4 项 Chrome 面积审核通过，原筛选/助手及面积换算 2 项通过 |
| `417c202` | 中文完全等价条件识别、结构矛盾及未识别文字提示，设施/市场条件硬性程度保留 | 56 项相关测试通过；中文 2 项及面积 4 项 Chrome 实操通过 |
| `4badf60` | 原文只有预算上限但结构字段另有下限时提示依据待确认，不自动清空下限 | 中文 22 项测试通过；新增预算下限 Chrome 实操 1 项通过 |
| `7531e76` | 纯 demo 复制工具、15 条参考对照工具、本地双端口运行、边界测试与交接 | 完整结果见下表 |

### 已实现行为

- 客户 `area_basis` 与房源使用相同枚举。旧字段缺省/空值可承接旧英文标记；显式 unknown、缺失和冲突分别保留，不从房源推断客户口径。字段冲突时表单显示已填值，实际比较仍 unknown；确认必须显式修订冲突依据。
- 销售可在已有需求上打开审核表单。修改面积口径后保留同客原需求和一份独立会话需求；原文与硬条件不自动删除。刷新整个页面清除会话需求，输入文件不变。
- 中文最低卧室、最低面积、明确预算上限只有数值、单位、币种及硬性含义完全相同时才算重复表达；页面解释字段映射。矛盾、约数、精确/最低户型、无依据预算下限、设施与市场硬性程度及未知文字继续提示。详见 [需求审核说明](requirement-review.md)。
- 确定性筛选仍由所有入口共用，双向匹配保留理由。结构候选带审核提示，不能当作已确认推荐；人工参考仅在结果生成后对照，不参与过滤或排名。

### 运行、样本和本地产物

本批暂存目录 `data/incoming/bhhs-v1-cb36120bce00/normalized/demo-prepared` 只读保留；按原字节复制到 `data/demo/intake-local/cb36120bce00/dataset.json` 后通过现有 `BHHS_DATA_DIR` 加载。原件、规范化输入、派生报告及这个运行副本均在 Git 忽略目录中；公开仓库不包含批次记录。

在 `E:\bhhs` 执行，现有依赖无需重复安装：

```powershell
node tools/prepare-demo-batch.mjs data/incoming/bhhs-v1-cb36120bce00/normalized/demo-prepared/dataset.json cb36120bce00
$env:BHHS_DATA_DIR = 'data/demo/intake-local/cb36120bce00/dataset.json'
$env:BHHS_API_PORT = '8002'
$env:BHHS_WEB_PORT = '5174'
npm run dev
```

批次网页 `http://127.0.0.1:5174`，API `http://127.0.0.1:8002`；默认主演示可继续在 `5173/8001` 运行。重启默认服务前清除以上三个环境变量，详见 [导入指引](import-guide.md)。纯 demo 复制要求五张原始表，不接受附加未审查内容，不覆盖不同已有副本。报告写出拒绝与输入重合及通过链接覆盖其他文件。

本轮批次接收 **49 条 demo**：7 个挂牌快照（6 个挂牌）、10 条成交、10 条关联、7 位客户的 7 份需求、15 条人工参考。**28 条公开候选仍全部隔离**，核验和许可状态未改，未混入运行数据；开发没有代产品访问来源或确认使用权。

对照工具命令：

```powershell
node --import tsx tools/compare-match-references.mjs data/demo/intake-local/cb36120bce00/dataset.json data/incoming/bhhs-v1-cb36120bce00
```

本地交付文件：

- `data/incoming/bhhs-v1-cb36120bce00/候选对照报告.md`：15 条逐案实际候选、状态、差异、原因和待补字段。
- 同目录 `candidate-comparison.json`：相同对照的机器结果、逐套评估、等价条件和成交分类。
- 同目录 `人工草稿差异复核.md`：15 条人工核对意见，另外核对草稿“满足/冲突”理由与原始字段；它是开发复核意见，不是产品签字。
- `outputs/area-basis-v1.1.0/BHHS_数据字段模板_v1.1.0.xlsx` 及 `data/templates/v1.1.0/`：可填写的新空模板。旧 v1 输入和旧空模板保留。

### 实际验证与限制

| 实际执行 | 结果和范围 |
|---|---|
| `npx.cmd tsx --test --test-reporter=spec tests/*.test.ts tests/*.test.mjs` | **100/100 通过，退出 0，无跳过**；覆盖新旧 CSV/JSON、空值/unknown/一致/冲突、中文等价/矛盾、复制和原件保护；其中批次工具 8 项，Windows 文件 symlink 权限不可用，实际以 junction 验证重定向拒绝，硬链接另有通过用例 |
| `npm run build` | 通过，TypeScript 通过；3013 模块；主 JS 1,042.52 kB、gzip 327.88 kB，仍有大包提示 |
| 原有两份 Chrome 用例合跑 | **15/15 通过，42.2 秒**；普通筛选/助手一致、单位、双向需求、日期排序、0/1/多笔成交、来源回跳、缺字段及加载/错误恢复 |
| 本轮新增 Chrome 用例 | **7/7 通过，分批执行**；`area-basis.spec.ts` 四项、`constraint-review.spec.ts` 三项；并非一次 22 项合跑 |
| 实际批次，无 mock 浏览器 | **6 步通过，8.15 秒，退出 0**；1366×768，无页面横向溢出，`pageErrors=[]` |
| 原件与隔离边界复核 | 原 Excel、主演示、prepared JSON、public-candidates JSON 的 SHA-256 前后相同；运行副本与 prepared 原字节相同；批次路径被 Git 忽略 |

实际批次 6 步包括加载七份需求；一份原需求显示面积口径待确认/0 候选；仅会话选定 built_up 后变为 2 候选且仍有原文待审信息；房源潜客保留同客两份独立需求并可返回原需求的 0 候选；两张挂牌快照只对应一笔同屋成交且可比独立；无历史房源显示空态；整页刷新恢复原七份需求。输入原文、硬条件、未知购房日期和输入哈希均不变。**会话选定口径仅用于技术验证，不表示产品已确认该客户口径。** QA 记录及五张辅助截图在 `.work/intake-browser-qa-20260903T073155Z/`。

本批七份需求都缺完整购房/入住日期，五个房源各一笔同屋成交、另一房源无历史。日期排序和多笔成交图沿用原有主演示用例验证，没有向本批补造日期或交易。规则助手未接入大模型；未做真实业务验收、移动端完整链路、多浏览器、线上更新或公网性能验证。

### 差异、产品待补及控制塔事项

15 条参考均为 **draft**，不计算业务通过率。本批有 6 份需求设置了面积下限但缺客户口径；6 条推荐/备选参考（对应其中 5 份需求）的目标仍未进入当前结构候选，不据此认定市场上无房可选。另有一份无面积下限需求返回 3 个结构候选，预算、币种、用途和时间仍需确认。全部逐案差异见本地报告，不把草稿改成程序答案。

| 负责人 | 待补或待确认 |
|---|---|
| 产品 B | 六份需求的客户面积口径；“1/3/4 居”是精确户型还是下限；“约…以上”的最低值/容差；仅原文上限时已填预算下限的依据；预算是否含费用；设施为硬条件还是偏好；完整购房/入住日期和相对时间依据 |
| 产品 B | 复核草稿中“预算内”“面积满足”“户型超出”和泛化无匹配理由；现有字段存在反例，不能以标签一致替代逐项确认。保留原 draft，确认后另交版本 |
| 产品 A | 单条挂牌和成交来源、精确金额、交易范围、许可、房屋身份和关联证据；有多笔同屋成交时按真实资料补充。28 条公开候选仍待核验，不用于本批展示 |
| 控制塔 | 接受 v1.1.0 兼容增量后组织产品复核；精确户型、容差或条件阈值若需扩展契约，另行决定。接收批次保持本地边界，替换公开数据需另行审查与授权 |

本轮有两处提交前工具审查问题并作修复：仅校验五表后复制整份 JSON 可能夹带额外内容；报告写出路径可能覆盖输入。相应守卫及原件不变断言纳入工具测试。浏览器脚本曾因来源 ID/ref 同值与空态文案产生定位歧义，修正测试定位后重跑通过，未把失败脚本算作通过。

本轮修改归属开发主代理及其受控子任务，提交前逐文件检查并串行提交；未改写历史。输入原件、运行副本、私有对照和 `.work` 验证产物按约定留在本机，不能为了清理工作区而提交这些内容。交接时的短 SHA 和 `git status --short` 以窗口最终实查为准。

## 本轮代码追加上线（2026-09-03 UTC）

用户在本地交接后明确要求“先推到线上”。据此把 `5d28532..7531e76` 的六笔提交推送到现有 `origin/main`，由既有 GitHub 集成触发 Vercel Production。没有新建项目、改写提交历史或替换公开数据源。

- 稳定地址：[bhhs-gulf-properties-demo.vercel.app](https://bhhs-gulf-properties-demo.vercel.app)。
- 本轮实现部署：`dpl_6T7Ss3GYaxPprz1xmFT4vaS3xAmN`，源提交 `7531e767fd7cba3939d3b2a55c09176c47d34297`；Vercel inspect 为 `production / Ready`，稳定别名已绑定，GitHub Vercel 状态为 success。
- 推送后 `git ls-remote origin refs/heads/main` 与本地实现提交一致。发布说明随后单独提交；应用代码与上述已验证实现一致。
- 本次重新运行 `npm run build:public` 通过，TypeScript、Vite 和固定快照导出均成功；公开导出边界测试 **4/4 通过**。大包提示仍在，没有把构建通过当作浏览器操作验收。
- 独立只读审查覆盖待推六次提交、53 个变更路径、54 个唯一文件版本及新增 Excel XML；未发现本地批次、环境文件或凭证模式命中。只提交空模板、明确虚构用例及源码文档。

公开数据仍是固定主演示：11 个挂牌快照、8 条成交、9 条关联、8 份需求、8 条参考，全部 demo、零隔离。49 条接收样本的运行副本、15 条草稿对照和 28 条公开候选仍留在本地忽略目录，未推送或部署。公开助手仍为规则模式，本次上线不改变产品待补和业务验收结论。

### 本次线上实操

全新匿名 Chrome 执行 `.work/verify-public-area-review.mjs`，**7 步通过，14.19 秒，退出 0**。首页 HTTP 200，无登录或重定向；正常页面数据请求为 200 JSON，加载的新 JS 为 `index-DdfQZFIL.js`、CSS 为 `index-D5Kam0V8.css`。

实际操作覆盖原需求 2 候选→面积口径 unknown 后 0 候选和待确认提示→恢复 built_up 后返回原两候选；同客户原需求和会话需求独立、分别回跳；浏览器会话内独立虚构中文预算矛盾在审核和应用后均有提示；同屋三笔与可比一笔分开，键盘定位来源、日期过滤 3→1→重置 3 正常。刷新清除三条会话测试需求，公开原需求逐字段不变。

1366×768 无页面横向溢出，页面错误、失败请求及非 GET 请求均为空。没有 route/mock，没有读取或上传接收批次；只是浏览器内的演示审核，未写回。证据与五张辅助截图保存在 `.work/public-area-review-qa-20260903T075326Z/`。未进行公网性能、地区可达性、多浏览器或移动端完整验收。

## 本机保存与案例复验准备（2026-09-03 UTC，本地）

本轮依据 `AGENTS.md` 与 `docs/solo-execution-brief.md` 执行，保留 `main@7531e76` 之后的两笔既有文档提交，从干净的 `bede79b` 开始。未改控制塔负责的 AGENTS、单人推进说明或产品输入。开发与受控子任务分配明确文件，主代理统一验证、检查差异并串行提交。

| 提交 | 已完成内容 | 对应验证 |
|---|---|---|
| `473b252` | API 返回不含本机路径明文的来源摘要；目录与其中 dataset.json 入口一致 | 18 项来源、接入与公开导出检查通过 |
| `67d736f` | 来源/五表版本键、独立副本关联、结构校验、修订冲突、写后回读确认 | 13 项保存模块测试通过，类型检查通过 |
| `78d71b1` | 新增/审核副本保存与恢复、同客多需求、删除/恢复原件、状态/错误界面和浏览器用例 | 全量 114 项及构建通过；新增 Chrome 6 项、原有回归及真实批次实操见下表 |

保存键只基于导入数据，副本不影响批次指纹。源目录或五表任意内容变化均隔离；加载时间变化不产生新版本。应用成功才激活新副本，失败留在表单。每份副本保存其原件及上级需求 ID，删除当前副本不删除同客其他需求。所有副本仍待业务确认，不修改人工参考、不自动补面积口径或日期。

### 本轮实际验证

| 执行 | 结果及范围 |
|---|---|
| `npx.cmd tsx --test --test-reporter=spec tests/*.test.ts tests/*.test.mjs` | **114/114 通过、退出 0、无跳过**；含存储损坏、读取/写入拒绝、回读失败、修订冲突、批次键、原件保护及保存前后相同匹配结果 |
| `npm run build` | **通过、退出 0**，包含 TypeScript；3015 模块，JS 1,053.79 kB / gzip 331.54 kB，保留大包提示 |
| `tests/browser/local-persistence.spec.ts` | **6/6 通过，约 1.1 分钟、退出 0**；真实控件操作，使用明确虚构的接口样本和写失败注入；覆盖新增与重复审核、刷新/重开标签、同源切批次与改版本、同客多需求删除/恢复、Quota/Security 失败保留草稿与重试、正反向理由及价格来源 |
| 原有四份 Chrome 文件（sales-flow、iteration-02、area-basis、constraint-review） | 首次 **21/22**；剩余 1 项在点击时 DOM 因 Vite HMR 重建而超时。trace 记录 `/src/App.tsx` 热更新，代码稳定后同一用例原样重跑 **1/1 通过，7.2 秒、退出 0**，未修改应用或断言来消除该失败。不是一次 22 项全绿执行 |
| 实际 49 条接收批次，5174，无 mock | **9/9 阶段，26.27 秒、退出 0**；同一独立 Chrome profile 共启动三次，每次关闭均确认 browser disconnected；副本保存后两候选，完整重开仍为同 ID 和相同正反向逐条理由；原件始终零候选且面积待确认；恢复原件后再次重开，副本消失、七份导入需求完整；同屋/可比来源分别通过控件展开 |
| 数据与桌面操作 | 默认样例、原 Excel、demo-prepared、public-candidates 四份 SHA-256 不变；API 五表全字段前后相同，49 条仍为 demo、15 条参考仍 draft、七份购买/入住日期仍未知；新增用例在 1366×768、其余回归默认 1440×1000 实操；实际批次无页面脚本错误和 API 写请求 |

本轮回归共覆盖 28 个唯一浏览器用例，按以上批次及单项重跑完成，不表述为一次 28 项合跑。接收批次的缺日期和多笔历史问题继续使用既有主演示测试覆盖，未向接收批次补造数据。

复现自动验证，先启动默认主数据服务；接收批次并非这组通用回归的测试数据：

```powershell
npx.cmd tsx --test --test-reporter=spec tests/*.test.ts tests/*.test.mjs
npm run build
$env:BHHS_E2E_BASE_URL = 'http://127.0.0.1:5173'
npx.cmd playwright test --reporter=list --output=.work/browser-revalidation
Remove-Item Env:BHHS_E2E_BASE_URL
```

本轮为避免与正在查看的服务混淆，自动浏览器用例实际使用额外主演示服务 `5175/8003`；用户默认服务为 `5173/8001`，实际接收批次为 `5174/8002`。现有服务已重启并含来源摘要。启动命令沿用本文件“运行、样本和本地产物”章节，未增加框架或服务种类。

私有技术证据均在忽略目录：

- `.work/persistence-final-unit.log`、`.work/persistence-build.log`：全量单元与构建输出。
- `.work/persistence-browser-results/`：新增浏览器用例产物；`.work/persistence-baseline-browser/`、`.work/persistence-area-rerun/`：原回归与 HMR 后重跑证据。
- `.work/verify-intake-persistence.mjs`、`.work/intake-persistence-qa-20260903T082423442Z/report.json`：真实批次操作、三次浏览器会话、逐条匹配结果及输入前后哈希。
- 旧 `data/incoming/bhhs-v1-cb36120bce00/` 的候选对照、人工草稿差异复核与原文件保持原样；本轮没有收到新版业务输入，未重生成这些报告。

### 演示设定与待用户补充

本机保存是技术能力，**未新增已确认的业务假设**。实际批次 QA 为验证保存，仅在专用浏览器副本上暂选 `built_up`，随后恢复原件并再次重开确认删除；不是用户或客户对口径的确认。虚构回归数据和失败注入另有明确测试说明，记录见 [本机保存的演示设定](local-requirements.md)。

| 当前由用户收口的事项 | 保留状态与下一步 |
|---|---|
| 客户面积口径及面积单位/近似表达 | 六份带面积条件需求仍缺口径；六条推荐/备选对应五份需求的目标未进入原结构候选。补充有依据的新版本，未知继续待澄清 |
| 卧室是精确户型还是最低值，预算下限依据/费用，设施硬性程度，购买/入住日期 | 原文与结构矛盾继续提示；精确户型、容差或其他新规则另行决定，不通过删除约束迎合草稿 |
| 15 条人工草稿理由及其状态 | 仍 draft；用户确认后另交版本，逐案比较实际候选与理由，不按草稿答案调匹配规则 |
| 28 条公开资料的核验、许可和房屋身份/成交关联 | 隔离不变；真实资料不能用演示设定替代证据。可用部分逐条接入，无需一次补齐全部资料 |

新版资料如何独立保存、运行及输出对照已在 [案例复验准备](case-revalidation.md) 写清。命令行工具仅比较该输入批次与其人工参考，不包含浏览器副本，也不自动比较两个输入版本。收到新版本后再按 ID 对齐旧/新报告，另存字段、候选、理由和依据的差异表；逐条报告继续留在忽略目录。

本轮未验证跨设备恢复、多浏览器或移动端完整交互，未做公网发布或性能验收。浏览器清理站点存储会丢失副本，没有服务器备份；不支持 Web Locks 时仅提供修订号检查，未宣称跨标签原子事务。规则助手、价格证据门槛和匹配规则未新增业务判断。最终交接核对本地提交及工作区，不 push 或部署。

## Home、销售身份与 Reports 改版（2026-09-03 UTC，本地）

依据用户附件 `20260903_BHHS房产管理Demo改版需求清单.md` 及后续“启动”授权，从干净的 `main@5f90622` 推进。保留 AGENTS、单人推进约定、原始输入与人工草稿。本轮无共享 CSV 字段改名，不改变 v1.1.0 接入契约；看房记录为独立的本机存储结构。

### 提交与实现

| 提交 | 独立改动及相应验证 |
|---|---|
| `d08470b` | 演示身份格式、保存/失败校验及 Sales ID 分区；3 项逻辑测试通过，记录执行范围 |
| `683d054` | 手动搜索面积上下限、sqft 界面与更新时间双向排序；11 项新测试及相关回归通过 |
| `fb55532` | 可见客户目录的姓名、预算交集、地区、公私过滤；7 项测试，条件必须来自同一份需求 |
| `e8ad1d2` | 本机看房记录、关联/修订校验、标签观察及同预算统计；15 项测试通过 |
| `77a78e0` | Reports 两个 Tab、复用价格证据、看房表单/时间线与来源说明；Chrome 6 项通过 |
| `6418762` | Home、销售身份与私有需求、导航深链接、房源库和目录集成；Chrome 8 项、最终构建及实际批次联调通过 |
| `a9035d9` | 原 5 份浏览器文件适配 Home、真实登录及 AED 范围，保留原业务断言；最终 28/28 通过 |

本文件及相关说明最后独立提交。最终交接时已提交全部本轮明确负责的代码和文档，没有借清理工作区暂存他人文件；没有 push、部署或重写旧提交。最终文档 SHA 以窗口交接和 `git log -1 --oneline` 为准。

Home 直接使用原需求提取与审核能力，首次登录前输入可保留。当前销售离开时清空其未保存 Home 草稿；已保存副本按来源版本和 Sales ID 恢复。导入演示需求标为 Company，新增及审核副本标为 Private，客户目录、候选下拉、反向潜客和 Reports 仅使用当前可见需求。旧无归属副本留在旧键，退出身份后可查阅或删除，不自动归给第一位销售。

房源库独立显示地区/类型，卧室和 sq ft 面积并列，区间即时生效。原样例有一套 USD 挂牌，因此采用明确提示的 AED 搜索视图，默认 active 为 8 套；USD 原价留在详情与 Reports，没有换汇或重标。宽表可横向滚动；列头排序按真实报价和捕获时间，空价不冒充零价。通过地址中的需求/挂牌 ID 恢复有效详情，不存在或无权查看的本地需求显示提示。

Reports 的房屋报告复用既有同屋/可比成交，不补造缺失历史。客户报告保存实际输入的看房、反馈、主动偏好标签，次数和规则观察随记录更新。同预算模块说明客户、需求及记录依据；样本不足时保留空态。**Load fictional viewing examples** 需主动点击且逐条标记虚构。操作与内部字段见 [报告记录](report-records.md)。

### 最终实际验证

| 执行 | 结果与证据边界 |
|---|---|
| 全量领域测试 | **150/150，退出 0，无跳过**；包含面积范围、身份存储、目录过滤、原件/版本保护、中文条件与价格证据、看房来源/统计 |
| 最终 `npm run build` | **退出 0**，含 TypeScript；3024 模块，JS 1,099.79 kB / gzip 344.27 kB，CSS 62.05 kB / gzip 12.17 kB；保留大包提示 |
| `workspace-v3.spec.ts` | **8/8，1.3 分钟，退出 0**；Home/登录、私有目录联合筛选、客户→候选→详情刷新、A/B 在目录/下拉/反向名单/深链接的隔离、同客多份删除恢复、面积区间和四向排序、未保存草稿隔离 |
| `reports-v3.spec.ts` | **6/6，55.6 秒，退出 0**；同屋 3/1/0 笔、可比独立、原 AED/USD、来源回跳；手工看房刷新、次数/标签/正面反馈分开、虚构示例、同预算证据、A/B 隔离、单键配额失败保留草稿及重试、详情返回时报告选择保留 |
| 既有 5 份浏览器文件 | **28/28，2.9 分钟，退出 0**；最终样式及身份修复后的原样复跑，覆盖旧链路、面积与中文冲突、加载/错误/空态、版本切换、存储失败、删除恢复和双向匹配 |
| 实际 49 条接收演示批次，5174、无 mock | **7/7 阶段，33.12 秒，退出 0**；原样复核副本维持未知口径、零候选及原文警示；独立 QA `built_up` 副本两候选，前后向逐条理由一致；刷新及同 profile 完全关闭 Chrome 后重新启动均恢复同 ID 和状态；详情/Reports 挂牌价及同屋/可比来源分别可操作 |
| 原件与桌面检查 | 四份受保护文件 SHA-256 全部不变，API 五表全部原字段不变；49 demo、15 draft、7 个未知购买日期保留。新工作区和 Reports 在 1366×768、旧回归默认 1440×1000 操作；无页面脚本错误，新增用例无文档水平溢出；实际批次无 API 写请求 |

共 **42 个唯一浏览器用例**，按 28 + 8 + 6 三组执行，不表述为一次合跑。途中旧控件名、Ant Design 图标及动画等待的测试假设已适配；未知预算弹性仍断言待澄清，未为通过测试修改业务状态。最终代码审查和截图还定位了两项真实缺陷，并保留修复前失败证据：

- 基础 CSS 加载晚于改版样式，面积两输入只有约 45px，未选客仍被侧栏挤压。调整加载顺序后分别为 117.56 / 117.58px，未选客摘要位于表格下方 20px；增加几何与实操断言。
- Home 编辑器未重建，A 未保存的原文/审核草稿会在切 B 或退出后保留。现在离开已有 Sales ID 重建编辑器；A→B、B→退出均清空，访客首次登录仍保留其输入。新增第 8 个工作区用例已通过。

运行上述浏览器验证前启动默认主演示数据，命令可为：

```powershell
Set-Location E:\bhhs
npx.cmd tsx --test --test-reporter=spec tests/*.test.ts tests/*.test.mjs
npm run build
$env:BHHS_E2E_BASE_URL = 'http://127.0.0.1:5173'
npx.cmd playwright test sales-flow.spec.ts iteration-02.spec.ts area-basis.spec.ts constraint-review.spec.ts local-persistence.spec.ts --output=.work/v3-legacy-browser --reporter=list
npx.cmd playwright test workspace-v3.spec.ts --output=.work/workspace-v3-browser --reporter=list
npx.cmd playwright test reports-v3.spec.ts --output=.work/reports-v3-browser --reporter=list
Remove-Item Env:BHHS_E2E_BASE_URL
```

本次通用浏览器回归实际使用 `5175/8003` 的独立 QA 主数据服务，默认用户入口 `5173/8001` 与接收批次 `5174/8002` 保留。启动仍为 `npm run dev`，接收批次环境变量见本文件既有启动章节与 README；没有新增服务类别。普通克隆不含私有接收批次。

私有/临时证据：`.work/v3-final-unit.log`、`.work/v3-final-build.log`；上述三组浏览器目录及 `.work/v3-pre-fix/`；`.work/verify-intake-v3.mjs` 和最终 `.work/intake-v3-qa-20260903T101304117Z/report.json`。报告记录两次 Chrome 会话的关闭和断开、逐条理由与源文件哈希。逐案内容与截图未进入 Git，旧人工对照报告未重生成。

### 演示设定、待用户补充及限制

没有将保存、登录或观察统计当成客户事实确认。导入演示客户共享、Sales ID 私有分区、同预算至少两位其他客户/两条观察是 Demo 展示设定；不是正式认证或冻结业务规则。实际批次 QA 面积选择仅存在于独立浏览器副本；客群正向用例仅在 B/C 的独立 QA 副本补预算下限，原件仍未知。模拟看房均标记 Fictional example，不修改原始需求、人工草稿或来源许可。

| 统一由用户收口 | 当前处理 |
|---|---|
| 客户面积口径、精确/最低户型、近似面积、预算下限/费用、设施硬性程度及购买日期 | 继续保留 unknown/原文冲突；6 条推荐/备选仍待信息补充，另交有依据的新版本后复验 |
| Ready/Active/Off-plan 及潜客状态最终字典 | 沿用现有完成状态、挂牌状态及三个条件分组，补总数公式和名单说明；未擅自重定义 |
| Registration date 等日期命名 | 沿用来源的 `date_basis` 合同/登记日期，未发现当前代码字面 Demo date 占位；待定义确认后统一术语 |
| 15 条人工参考及 28 条公开资料的核验/许可/房屋关联 | draft 和隔离均不变，不据草稿答案改过滤，不把公开可见当成授权 |
| 真实客户访问范围和看房证据 | 本轮演示身份可自行选择，只有本机界面分区；需要真实认证与来源授权时另行接入 |

尚未验证正式认证、跨设备/跨浏览器恢复、移动端完整交互或公网性能；没有服务器备份。Reports 的看房保存有修订号与回读检查，不声称跨标签原子事务。规则助手和报告均未训练或调用模型，没有综合分数、成交概率、客户资产推断、CRM 写回或消息外发。收到新版输入后按 [案例复验准备](case-revalidation.md) 另存差异，原始记录及旧人工草稿保留。
