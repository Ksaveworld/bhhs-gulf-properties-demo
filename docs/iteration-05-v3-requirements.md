# BHHS 房产销售工具 Demo 改版需求清单（V3）

> 最新口径：2026 年 9 月 4 日《产品demo细节优化讨论》，材料标注时间为 14:45–15:06（GMT+08）。
> 本文沿用 V2 的页面结构，合并本次增量；与 V2 冲突时，以本次逐字记录的最后明确结论为准，未变更的字段和业务规则继续沿用 V2。
> 来源：用户提供的《文字记录：产品demo细节优化讨论 2026年9月4日.md》和同名智能纪要。逐字记录为主要依据，智能纪要用于交叉检查；下文时间戳均对应逐字记录。
> “已确认”表示会议明确要求，不表示代码已实现、数据已核验或业务已验收。实现方式另行标明。本轮按用户要求整理文档并修改代码，本地验证、分步提交。

## 一、本次会议覆盖上一版的关键结论

- **演示标识集中到 Home 旁**：删除各页面重复的 `Demonstration only`、`Demo data`、规则演示说明和房产 / 客户名称前的 `Demo`；保留 Home 旁一个 `Demo` 标识。不是“整站完全不标演示”。【04:38–05:18】
- **房产报告入口移入详情**：删除 Property Library 最后一列 `Export Report`，移到 Property Details 右上角，使用醒目按钮；报告内容和 PDF / Word 能力延续 V2。【03:31–04:29】
- **连续查看详情保留主页面**：房产 → 客户 → 其他房产均在当前页面查看，关闭当前详情返回上一层详情，不能切换背后的导航页。【14:37–18:02】
- **灰色功能改为可操作**：`Reviewed by local sales` 与 `Add Private Client` 提供可演示的完整交互；后者在 Clients and Needs 当前页内完成。【06:32–07:56、19:34–19:59】
- **来源去重，保留有效回跳**：Overview 的 Source / Source Reference 合并，来源链接保留；删除 Overview 及成交详情的 `Original Evidence` 重复段落。【05:53–08:25、10:20–10:35】
- **修复时间线和潜客卡片**：时间线不再窄缩靠左，展开后可收起；潜客卡片右上角用明显的 `View Client Details` 替代重复状态标签。【09:01–11:43、11:55–14:23】

---

## 二、Home / 首页

### 1. 三个自然语言任务入口【已确认，延续 V2】

保留 `Find a Property`、`Find a Client`、`Create a Private Client` 三种任务及输入框前的任务 Tag。三种任务仍分别生成对应的可编辑表单，不改为同一无差别流程。

### 2. 示例直接放入输入框【已确认】

- 将三种任务的 `Describe what you need…` 替换为各自的具体示例，放在输入框内部。
- 删除输入框下方重复的灰色示例和 `Rule demo · Review the suggested details.`。
- 示例随任务切换；保留简短任务说明。

**本轮实施选择**：使用 placeholder 展示示例，不把示例写成销售实际输入，不自动发送，不覆盖正在填写的正文。

**验收口径**：三个任务的输入框示例不同；输入后示例消失；清空后恢复对应示例；发送、填表与 Continue 链路正常。【00:19–00:57】

### 3. 字段、必填校验和后续动作【已确认，延续 V2】

| 任务 | 必填条件 | 后续动作 |
| --- | --- | --- |
| `Find a Property` | 区域、最高预算、房产类型、卧室 | 带条件进入 Property Library |
| `Find a Client` | 姓名、偏好区域、预算、房产类型中至少一项 | 带条件进入 Clients and Needs |
| `Create a Private Client` | 客户名称、区域、最高预算、房产类型、卧室 | 展示完整字段二次确认；确认后创建并打开详情 |

- 未提取出的必填项继续显示星号、红框和缺失数量；不能因精简界面默认放行。
- 面积、购房 / 入住日期、设施、偏好、待澄清问题等可选字段继续保留，无需为了本次演示新增必填限制。
- 继续使用 `Continue`、`Back / Edit`、`Confirm & Create`。
- 币种与单位沿用 V2 的 `AED`、`sq ft`；缺失面积口径保持待确认，不根据候选房源反填。

**验收口径**：缺失必填字段时不能继续；补齐后可继续；私有客户仍需二次确认。【01:15–02:09、02:47–02:53】

### 4. 日期与英文展示【已确认】

- 修复 `Purchase By` 等日期输入仍显示“年 / 月 / 日”的问题。
- 系统字段、提示、按钮、日期控件和错误信息统一为英文；不能只修改页面语言属性而保留系统本地化控件的中文占位符。
- 原始客户输入、原始来源内容仍按原文保存；不把翻译或演示改写当成原始事实。

**本轮实施选择**：统一使用明确的英文日期提示 / 格式，覆盖需求审核和看房日期入口；日期值继续遵循现有契约，不改变日期与时间的含义。

**验收口径**：在中文浏览器环境下实际打开、输入和清空日期，系统控件显示英文；刷新后日期值不漂移。【02:09–02:47】

---

## 三、Property Library / 房产库

### 1. 筛选字段与状态【已确认，延续 V2】

保留即时筛选、价格 / 面积区间、地区、类型、卧室、建设 / 挂牌状态及 Status Tooltip。挂牌价与成交价、Ready / Off-plan 与 Active / Withdrawn / Sold 的含义继续分开。

### 2. 表头排序样式【已确认】

- `Asking price`、`Updated` 的上下排序箭头紧邻字段名，缩小上下与横向间距，与标题垂直居中。
- 保留当前字段 / 方向高亮；默认 Updated 从新到旧。
- 本次改变样式，不改价格和更新时间的排序规则。

**验收口径**：两个方向可操作、结果排序正确；桌面尺寸下箭头无明显偏下、分散和错位。【03:12–03:35】

### 3. 名称、图标和行交互【已确认 + 实施选择】

- 房产名称不再重复加 `Demo` 前缀；地区、卧室、面积仍在各自字段显示。
- 删除会议指出的无意义小图标。【18:49–18:51】
- **记录差异**：03:35 曾说箭头可以保留；18:51 最后要求删除 Property 字段内容右侧的小图标。现有代码中名称旁是装饰楼宇图标，右侧箭头是独立列，文字记录不能精确对应截图位置。
- **本轮实施选择**：删除这两个非必要图标及独立箭头列，保留房产名称作为明确的详情入口。整行仍不可点击，避免复制文字时误触。

### 4. 房产报告入口迁移【已确认】

删除 `Export Report` 整列，不保留列表中的第二套导出入口。导出使用当前打开的房产，无需再次选房。【03:35–04:29】

---

## 四、Property Details / 房产详情

### 1. 标题和报告按钮【已确认】

- 房产标题使用清理后的展示名称。
- 原右上角 `Demo data` 位置替换为醒目的 `Export Report` 按钮。
- 保留 Overview、Price evidence、Potential clients 三个页签及核心房产信息。

**验收口径**：从任意业务入口打开房产，右上角均可导出此房产的 PDF / Word，标题和内容对应正确。【03:57–04:38】

### 2. Overview：来源与人工确认【已确认】

- `Source` 与 `Source Reference` 合并为一个可理解的来源位置，直接放原本有效的来源链接；不再重复内部来源编号。
- 保留有用的房产关联、来源日期、来源核验状态；无来源链接时显示未提供，不编造链接。
- 删除 Overview 的 `Original Evidence` 及重复证据文案。
- `Reviewed by local sales` 必须可点击，可确认并取消；继续保存确认时间与 Sales ID。
- 删除勾选框下方原有的登录提示和重复说明段落；失败时仍显示具体错误，不能假装保存成功。

**本轮实施选择**：保留现有本机销售身份机制。访客点击确认时在当前上下文选择 Username + Sales ID，完成后返回当前房产再确认；不暗中分配销售、不自动勾选，也不把本机勾选改成来源核验通过。来源核验和人工确认字段保持独立。

**验收口径**：访客可以发起交互；选择身份后可确认、刷新恢复、取消；来源状态不因勾选改变；真实 URL 可回跳，缺链接时无假链接。【05:31–08:25】

### 3. Price Evidence：时间线布局与收起【已确认】

- 保留价格折线图和成交时间线；同屋历史在前，可比房产成交在后。
- 点击折线图节点后定位并展开对应成交记录；时间线和展开内容使用可用宽度，不挤在左侧留下大块空白。
- 展开后可以再次收起，并能继续选择其他节点；不因图表选中状态反复强制展开。
- 默认节点显示日期、金额 / 币种、交易类型及简短摘要，不再只有价格。
- 展开内容保留有用交易信息、房屋关联 / 可比依据与来源入口；删除 `Source Record` 和重复 `Original Evidence` 段落。
- 摘要只组合记录中已有字段，不虚构买卖双方、成交故事或额外历史。

**验收口径**：图表点选 → 正确节点展开 → 手动收起 → 其他节点展开均成功；在 1366×768 下内容可读、无窄列或横向溢出；无历史仍显示正常空态；不同币种和可比记录不混入同屋走势。【09:01–11:43】

### 4. Potential Clients：分组与卡片【已确认】

- 保留 `Condition Met` 和 `Needs Clarification` 两组，用组标题、细线或颜色明显区分。
- 删除每张客户卡片右上角重复的状态 Tag，不再通过拉长标签区分组别。
- 将 `View Client Details` 从卡片底部移到右上角，使用足够明显、可点击的正常尺寸按钮。
- 客户名称不再带重复的 Demo 前缀。
- 保留核心诉求、预算适配、可满足项、待澄清项、看房情况、已知购买时间和简短 Next Action；不改变匹配规则。

**验收口径**：两组清楚可辨；卡片无被标签撑出的空白；按钮位置一致；进入客户后仍可追溯原来的匹配条件。【11:55–14:23】

### 5. 客户与房产连续查看【已确认】

- 从 Property Library 打开房产 → Potential clients 查看客户 → 查看该客户推荐或看过的另一套房产，主页面始终保留 Property Library。
- 点击关闭或返回，逐层回到刚进入当前详情之前的上下文。
- 同样规则用于从 Clients and Needs 开始的连续查看；不因为查看关联对象改变主导航页。
- 返回时保留上一层页签、选中的客户需求及原页面筛选位置。

**本轮实施选择**：只让当前层详情可操作，上一层保留状态并暂时隐藏；当前层关闭后恢复上一层。会议讨论过窄抽屉叠加和替换显示，但没有冻结宽度和同时显示两个关闭按钮的视觉方案。

**验收口径**：实际完成至少三层连续查看与逐层关闭；背景页面和筛选不变；关闭全部详情后可继续操作列表。浏览器前进 / 后退不产生错误对象或遗留遮罩。【14:37–18:02】

---

## 五、Clients and Needs / 客户与需求

### 1. 筛选、分类与客户详情【已确认，延续 V2】

保留 Client Name、Preferred Location、Budget Range、Property Type 四项筛选，以及 All / Company / Private / Unassigned 分类。Client Details 继续使用 Recommended Properties、Viewing History 两页签；保留多份独立购房计划、审核副本、历史版本、删除与恢复及客户报告导出。

会议认为预算和客户分类展示可接受，本轮不重做客户卡片整体结构。【19:13–19:23】

### 2. Add Private Client 在当前页可用【已确认】

- `Add Private Client` 不再对访客显示为不可点击的灰按钮。
- 点击后在当前 Clients and Needs 页面打开创建流程，参考并复用 Home 的创建字段、自然语言填表和二次确认；不把页面跳回 Home。
- 新建成功打开该客户详情；取消后回到原客户列表，原筛选保留。
- 未选择销售身份时允许准备和审核草稿，保存阶段再完成身份选择；保存仍归属当前 Sales ID。
- 保存成功、失败提示与现有浏览器保存机制一致；不会覆盖导入记录。

**验收口径**：访客可点击并填表，身份选择后草稿不丢失；二次确认后创建、刷新恢复、切换身份隔离；取消返回原列表。【19:34–19:59】

---

## 六、Reports / 报告导出

- 继续取消独立 Reports 页。
- 房产报告从 Property Details 右上角导出；客户报告继续从 Client Details 右上角导出。
- 保留 PDF / Word 和 V2 的业务字段，房产 / 客户展示名称同步清理。
- 导出文件会脱离网页单独流转，保留简短的演示数据性质说明，避免虚构价格、成交和客户被当作真实事实；不复制界面已删除的大段说明。

**验收口径**：实际下载两个对象的 PDF / Word；文件非空、对象正确，询价与成交、同屋历史与周边可比仍分开。【03:35–04:29；文件数据性质为本轮实施边界，会议未要求变更】

---

## 七、全局文案、导航与数据边界

### 1. 文案与导航清理【已确认】

- 移除各页顶部 `Demonstration only` / `Data & sources` 说明条。
- 移除左侧 `Data & sources` 导航及关联说明弹窗。
- Home 旁保留一个 `Demo` 标识；移除其他重复演示标签、名称前缀与页脚重复字样。
- 保留销售身份和正常业务提示、错误、待澄清项；不把“删除无意义说明”扩展成清除业务异常。
- 最后的讨论允许保留底部销售身份文字，不据此前面的“删底下字”删除整个身份区。

【00:19–00:36、02:58、04:38–05:18、20:06–20:27】

### 2. 数据和事实边界【延续项目约定】

- 仅清理展示层名称、标签和重复字段；原始 ID、原始输入、来源引用、数据性质、核验和许可状态不改写。
- 既有纯演示数据继续使用；不混入待核验公开资料和私有批次。
- 本机保存、销售人工勾选、匹配结果和人工 draft 参考均不是已完成真实业务验收。
- 沿用规则助手，不宣称已接入大模型，不新增综合评分、成交概率或客户购买力推断。

---

## 八、本轮明确不做

- 不新增生产登录、审批流、服务器客户存储或 CRM 写回。
- 不更改匹配、面积口径、价格证据准入和客户归属规则以迎合展示结果。
- 不编造时间线摘要、日期、历史成交、来源和业务验收结论。
- 不回到独立 Reports 页，不重新设计已认可的三任务、筛选条件和客户两页签。
- 不从会议中的“演示”“今天展示”推断正式上线时间或外发授权；本轮先完成本地开发。

---

## 九、本轮补充确定的实施口径

1. 示例采用输入框 placeholder；不默认发送、不冒充实际需求。
2. Demo 标签集中于 Home 旁；底层证据保留，独立文件继续有简短性质说明。
3. 房产名称为表格详情入口；删除当前装饰楼宇图标和独立箭头列。
4. 连续详情采用保留上一层状态的回退栈；主页面不跳转。
5. 访客可启动确认 / 创建；选择销售身份后继续操作，不默认生成身份或替销售确认。
6. 日期控件使用明确英文格式；看房时间继续按现有时间存储规则处理。
7. 时间线摘要来自现有字段；来源保留一个有效入口，缺失仍显示未提供。

以上为技术实施选择，不冒充会议已经冻结的业务规则。

---

## 十、待确认事项与证据限制

- 会议未指定新的交付负责人承诺、完成日期或正式数据接入日期；本轮执行人由用户当前请求明确为开发窗口，业务问题统一收口用户。
- 会议提到核心流程现场可用，只作为会议观察；本轮改动必须重新实际操作验证。
- 10:42 对成交资料网站数量和真假比例的说法被发言者自述为猜测，不进入数据判断规则。
- 当前文字记录不能精确证明 18:51 所指图标的屏幕位置，按第三节的具体实现处理，不阻塞其他开发。
- 原有产品样本缺失面积口径 / 购买时间、多笔同屋成交以及 draft 人工参考的依赖继续保留；本次会议没有交付新数据或确认这些条件。

---

## 十一、实施与验证顺序

1. 先完成 V3 需求文档和 V2 差异核对，单独提交。
2. 清理展示文案、来源重复、表头排序和报告入口；同步使用位置。
3. 实现详情逐层回退与当前页创建流程，保留本机存储和身份隔离。
4. 修复时间线、潜客布局和英文日期输入。
5. 在目标桌面尺寸执行三任务、筛选、详情连续查看、创建 / 保存 / 恢复、来源、时间线与下载测试；保留测试证据。
6. 分功能提交，更新开发交接，报告实际结果、未验证范围和剩余业务依赖。
