# 按用户 HTML 与修改方案实现

依据：用户本轮提供的 `F:/Downloads/bhhs-agent-demo.html` 和 `F:/Downloads/BHHS-Demo-修改方案.md`。基线 `40fd326`；附件作为页面规格，不作为真实客户、真实外呼、外发消息或公网发布授权。

## 页面契约

- 现有导航保留，顺序 Home → Clients & needs → Property library；Demo 标记移至首页正文。
- 首页按 HTML 的标题、材料框和客户行呈现；Needs you today、新建姓名/电话入口；五条分析消息，读取数量逐项出现，身份和两项冲突必须人工选择，付款方式可稍后确认。返回首页保留对话。
- 客户列表保留原筛选与列表组件；房源库组件不修改。新增独立虚构 Khalid 记录，原数据不替换。
- 所有客户详情使用导航右侧完整页面。六项核心信息前置，再展示已知信息、相似客户、推荐房源、价格、风险、跟进和动作。原客户的版本、看房记录及报告保留在详情下方入口。
- HTML 与方案的文案差异按修改方案处理：Frond N 挂牌价 22.5m；价格轴按数值递增绘制 19.6 / 20.2 / 20.8 / 22.5，并标注可比区间和客户上限。
- 付款未确认时，全款及 30 天交割为条件建议；19.6m 保留为方案演示底线，不能解释为已核验估值或转售保证。Frond K / Sector E 缺少精确报价预设，切换显示各自挂牌、匹配条件及需补资料，不借用 Frond N 的数字。
- 通话面板自动计时，挂断自动进入可编辑总结；Changed 字段是写入画像及预约预填的字段。保存才新增通话来源、更新付款/预算、消除对应待确认风险并改变跟进。
- 预约从最近保存的电话总结预填；修改时间、房源和陪同人后可通过 Email / WhatsApp 模拟发送并留下结果。

## 演示边界

所有附件示例、来源原话补足、历史客户、匹配百分比和价格均为独立虚构预设。首页材料按钮选择此预设包；拖入文件仅保留文件名，不假装已转写真实录音或识别图片。销售笔记保留为独立待核对来源。真实列表客户不套入 Khalid 的历史与价格。

实现阶段仅本机运行与本地提交；后续用户授权上线，发布情况见下文。演示对话、来源、画像编辑、通话及预约回执保留在当前页面会话；刷新重置。原浏览器保存记录和销售身份隔离逻辑保留；不接入真实电话、CRM、邮件或 WhatsApp。

## 验证

- 类型检查和构建通过。197 项逻辑测试通过；新增的 3 项覆盖通话保存前后隔离、编辑值及来源一致性、连续通话历史、付款清空、普通客户不继承演示事实及编辑后的匹配输入。
- 9 项新浏览器场景分批通过：渐进读取和必选确认、独立新客户、来源/价格/编辑、自动通话与四处回流/预约、付款清空/无通话预约、保留导航/客户列表/房源库、390px 布局、原客户多计划和嵌套返回、列表检索及空客户字段。原工作台浏览器用例替换为当前合同用例。
- 房源库原有 4 项浏览器回归通过：价格时间线、来源字段、真实链接行为、潜在客户分组。
- 人工查看了首页、详情、来源高亮、桌面价格轴及手机价格轴截图。手机价格轴限于组件内横向滚动，页面无横向溢出。
- 构建仍有现有资源包大于 500KB 的提示。实现阶段未执行完整历史浏览器套件（其中仍有面向旧详情抽屉的定位）；未验证外部真实服务。

启动方式仍为项目根目录 `npm run dev`，本地地址 `http://127.0.0.1:5173/`。

## 正式发布 · 2026-09-06

用户明确要求“上线”后，将应用提交 `b5176d9` 发布到现有 Vercel 项目 `bhhs-gulf-properties-demo`。部署 ID：`dpl_8uwCUmyVt4bMH31YbdTZVY3wZBhZ`，目标 production，状态 READY。

正式地址：[BHHS Gulf Properties Demo](https://bhhs-gulf-properties-demo.vercel.app/)。初次发布只自动更新带账号后缀的域名；验收发现原正式地址仍指向旧部署，已显式重新绑定，并通过正式域名重跑检查。

- 云端 `build:public` 成功；公开数据检查通过，全部为 demo、隔离记录为 0。
- 未登录 Chrome 在正式域名完成三项浏览器验收：分析和确认链路、来源/价格切换/编辑、自动通话总结/四处回流/修改预约并模拟发送。
- `/api/health`、`/api/dataset` 均 HTTP 200；页面无捕获到的运行错误；主资源为 `index-CUiKMeKj.js`，与本地已验证构建一致。
- 本次未推送 Git 分支、未改变真实服务接入范围。发布记录为后续文档提交，不改变已部署应用代码。
